version: '3.8'

services:
  aurameal-agent:
    build: .
    ports:
      - "${PORT:-8080}:8080"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}
      - PORT=8080
    volumes:
      - ./aurameal.db:/app/aurameal.db
    restart: always
